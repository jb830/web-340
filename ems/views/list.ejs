
  </table>
  <% if (employees.length) { %>
  <h2 class="pb-3">Employees</h2>
  <table class="table table-bordered table-hover">
    <thead class="bg-info text-light">
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>View Record</th>
      </tr>
    </thead>
    <tbody>
      <% employees.forEach(function (entry) { %>
      <tr>
        <td><%= entry.firstName %> </td>
        <td><%= entry.lastName %> </td>
        <td><a href="/view/<%= entry.firstName %>.ejs">VIEW</a></td>
      </tr>
    </tbody>
    <% }) %>
  </table>
  <% } else { %>
  No entries! <a href="/new" class="btn btn-info">Add one!</a>
  <% } %>
  </div>
  <a href="/new" class="btn btn-info float-right">Add New</a>